# P0 清尾 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在上线与文档重构之前，把 4 个已确证的代码缺陷、1 个依赖积压、2 项工程空白清掉，使 `main` 处于"可以安全上线"的状态。

**Architecture:** 全部改动都是点状修补，不引入新架构。唯一有设计含义的是 Task 1：接管 ASP.NET Core `[ApiController]` 的自动模型校验响应，使"统一响应体"从**多数情况成立**变成**无条件成立**。

**Tech Stack:** .NET 10（SDK 10.0.401，仅在 Windows 侧，WSL 里须用 `/mnt/c/Program Files/dotnet/dotnet.exe`）· xUnit 集成测试（真 PG）· Vue 3（仅一处 CSS）· GitHub Actions

**Spec:** [`./2026-09-14-大整理与上线方案.md`](./2026-09-14-大整理与上线方案.md)（§2.4 缺陷清单、§4 P0 清单）

## Global Constraints

- 仓库行尾一律 **LF**（`.gitattributes` 的 `* text=auto eol=lf`），CI 的 hygiene 作业会拒绝 `w/crlf`。
- 提交信息用中文、`type(scope): 标题` 格式（见 `docs/07` §7.3）。
- **不新增业务功能**；不做前端工程化；不引入 Prometheus/OTel/ESLint。
- 所有测试必须跑在**真 PostgreSQL** 上（`BlogApiFactory` 会从 `appsettings.Development.json` 读连接串，指向已在运行的开发容器 `pgsql`，端口 5432）。
- 每次提交前 `git status` 必须确认没有误纳入 `优化的问题.txt`、`简历/`、`TODO.md`（它们已在 `.gitignore` 里，但**不要**用 `git add -A`）。
- 全部命令在**仓库根目录** `D:\dotNET项目\Stage2`（WSL 路径 `/mnt/d/dotNET项目/Stage2`）执行。

**统一命令前缀**（WSL 里 dotnet 不在 PATH）：

```bash
DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
```

---

### Task 0: 建立测试基线

**Files:**
- 只读，不改任何文件

**Interfaces:**
- Produces: 一个"改动前是绿的"的基线结论 —— 后续每个任务的回归判断都以它为准。

- [ ] **Step 1: 确认开发 PG 容器在跑**

```bash
docker ps --format '{{.Names}}\t{{.Status}}' | grep pgsql
docker exec pgsql psql -U kky -d blog_stage2 -tAc "select count(*) from \"Posts\""
```

Expected: 容器 `Up`，且能查到文章数（当前应为 13 篇）。若容器没起，先 `docker start pgsql redis`。

- [ ] **Step 2: 跑全量测试**

```bash
DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
"$DOTNET" test Blog.Backend/Blog.Backend.slnx --configuration Release 2>&1 | tail -20
```

Expected: `已通过! - 失败: 0, 通过: 195`（数字以实际输出为准，**记录下来**；这是基线）。

> ⚠️ **先杀正在运行的后端进程**，否则 MSB3027/MSB3021（文件被占用）。见 `docs/07` §2.4：
> `taskkill.exe /F /IM Blog.WebApi.exe`（当前 `Blog.WebApi.exe` 正在 5131 端口运行）。

- [ ] **Step 3: 记录基线数字**

把上一步的输出（通过数/失败数/耗时）记到 `plan/2026-09-14-P0执行记录.md`（本任务创建该文件，只写一行结论即可）。

---

### Task 1: 统一响应体缺口

**Files:**
- Create: `Blog.Backend/Blog.Tests/Integration/UnifiedErrorResponseTests.cs`
- Modify: `Blog.Backend/Blog.WebApi/Program.cs:39`（`builder.Services.AddControllers();` 之后）

**Interfaces:**
- Consumes: `Blog.Application.Common.ApiResponse.Fail(int code, string message)` → `ApiResponse<object?>`；`Blog.Application.Common.ErrorCodes.InvalidArgument` = 4001；`Blog.Tests.Infrastructure.ApiClient`、`Codes`、`BlogApiFixture`、`BlogApiCollection`
- Produces: 所有 `[ApiController]` 模型校验失败都返回 `{code:4001,message:"…",data:null}` + HTTP 400

- [ ] **Step 1: 写失败的测试**

创建 `Blog.Backend/Blog.Tests/Integration/UnifiedErrorResponseTests.cs`：

```csharp
using System.Net;
using System.Text.Json;
using Blog.Tests.Infrastructure;

namespace Blog.Tests.Integration;

/// <summary>
/// 「统一响应体」的边界测试。
///
/// <para><b>为什么需要这一组</b></para>
/// docs/03 §2.1 与 docs/05 §2 都承诺「<b>所有</b>接口（含错误）返回 {code,message,data}」。
/// 但 <c>[ApiController]</c> 的自动模型校验发生在 Action <b>之前</b>，
/// 默认输出 RFC 7807 <c>ProblemDetails</c>（{type,title,status,errors,traceId}）——
/// 于是同一个 API 并存两种错误结构。
///
/// <para><b>怎么发现的</b></para>
/// 手工验证全文检索时漏传 <c>keyword</c>，返回的是：
/// <code>{"type":"https://tools.ietf.org/html/rfc9110#section-15.5.1","title":"One or more validation errors occurred.","status":400,"errors":{"keyword":["The keyword field is required."]},...}</code>
///
/// <para><b>为什么断言"没有 type/title"而不只是"有 code"</b></para>
/// 只断言 <c>code == 4001</c> 的话，一个「两种结构都塞进去」的实现也能变绿。
/// 必须钉住<b>不含</b> ProblemDetails 的字段。
/// </summary>
[Collection(BlogApiCollection.Name)]
public sealed class UnifiedErrorResponseTests
{
    private readonly ApiClient _api;

    public UnifiedErrorResponseTests(BlogApiFixture fixture) => _api = new ApiClient(fixture.CreateClient());

    /// <summary>断言响应体是统一结构，且不是 ProblemDetails</summary>
    private static void AssertUnifiedErrorBody(string raw, int expectedCode)
    {
        using var doc = JsonDocument.Parse(raw);
        var root = doc.RootElement;

        Assert.True(root.TryGetProperty("code", out var code), $"缺少 code 字段：{raw}");
        Assert.Equal(expectedCode, code.GetInt32());

        Assert.True(root.TryGetProperty("message", out var message), $"缺少 message 字段：{raw}");
        Assert.False(string.IsNullOrWhiteSpace(message.GetString()), $"message 不能为空：{raw}");

        // 反面断言：ProblemDetails 的特征字段一个都不能出现
        Assert.False(root.TryGetProperty("type", out _), $"不应出现 ProblemDetails 的 type：{raw}");
        Assert.False(root.TryGetProperty("title", out _), $"不应出现 ProblemDetails 的 title：{raw}");
        Assert.False(root.TryGetProperty("errors", out _), $"不应出现 ProblemDetails 的 errors：{raw}");
        Assert.False(root.TryGetProperty("traceId", out _), $"不应出现 ProblemDetails 的 traceId：{raw}");
    }

    [Fact]
    public async Task 缺少必填查询参数_返回统一响应体()
    {
        // /api/posts/search 的 keyword 是必填的（PostsController.Search 的第一个参数无默认值）
        var res = await _api.SendAsync(HttpMethod.Get, "/api/posts/search");
        var raw = await res.Content.ReadAsStringAsync();

        Assert.Equal(HttpStatusCode.BadRequest, res.StatusCode);
        AssertUnifiedErrorBody(raw, Codes.InvalidArgument);
        Assert.Contains("keyword", raw); // 提示里要能指出是哪个参数
    }

    [Fact]
    public async Task 请求体类型不匹配_返回统一响应体()
    {
        // 给一个"合法 JSON 但不是对象"的请求体：模型绑定会失败，走的是同一条工厂路径
        var res = await _api.SendAsync(HttpMethod.Post, "/api/auth/login", body: "this-is-not-an-object");
        var raw = await res.Content.ReadAsStringAsync();

        Assert.Equal(HttpStatusCode.BadRequest, res.StatusCode);
        AssertUnifiedErrorBody(raw, Codes.InvalidArgument);
    }

    [Fact]
    public async Task 正常请求不受影响_仍返回统一成功体()
    {
        // 反向护栏：接管工厂不能把正常路径也改坏
        var res = await _api.SendAsync(HttpMethod.Get, "/api/site/config");
        var raw = await res.Content.ReadAsStringAsync();

        Assert.Equal(HttpStatusCode.OK, res.StatusCode);
        using var doc = JsonDocument.Parse(raw);
        Assert.Equal(Codes.Ok, doc.RootElement.GetProperty("code").GetInt32());
    }
}
```

- [ ] **Step 2: 跑测试，确认失败**

```bash
DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
"$DOTNET" test Blog.Backend/Blog.Tests/Blog.Tests.csproj \
  --filter "FullyQualifiedName~UnifiedErrorResponseTests" 2>&1 | tail -25
```

Expected: **前两个用例 FAIL**，报 `缺少 code 字段：{"type":"https://tools.ietf.org/html/rfc9110#…`。
第三个用例（正常请求）应当 PASS——它证明"问题只出在模型校验路径"。

- [ ] **Step 3: 实现**

修改 `Blog.Backend/Blog.WebApi/Program.cs`：

(a) 在文件顶部的 using 区加入（其余 using 已存在）：

```csharp
using Microsoft.AspNetCore.Mvc;
```

(b) 把第 39 行：

```csharp
    builder.Services.AddControllers();
```

改为：

```csharp
    builder.Services.AddControllers();

    // ---------------------------------------------------------------- 统一错误响应
    // [ApiController] 会在 Action 执行**之前**做模型校验，失败时直接短路返回 400。
    // 它默认输出 RFC 7807 ProblemDetails（{type,title,status,errors,traceId}），
    // 与项目对外承诺的 {code,message,data} 是**两套结构**。
    //
    // 后果不是"不好看"，而是调用方必须写两套解析逻辑：
    // 前端拦截器按 {code,message} 处理，一旦某次请求在校验阶段就被拦下，
    // 它拿到的是 {title,errors} —— 于是用户看到的是空白或 undefined。
    //
    // 这里接管这个工厂，让"统一响应体"从**多数情况成立**变成**无条件成立**。
    // 参见 docs/14 与 plan/2026-09-14-大整理与上线方案.md §2.4 A。
    builder.Services.Configure<ApiBehaviorOptions>(options =>
    {
        options.InvalidModelStateResponseFactory = context =>
        {
            var firstError = context.ModelState
                .FirstOrDefault(entry => entry.Value?.Errors.Count > 0);

            // 字段名为 "" 或 "$" 表示错误落在整个请求体上（如 JSON 格式不合法），
            // 这时说「参数 X 不合法」会误导 —— 请求里根本没有这个参数。
            var message = firstError.Key switch
            {
                null or "" or "$" => "请求体格式不正确",
                var field => $"参数「{field}」不合法或缺失",
            };

            return new BadRequestObjectResult(
                ApiResponse.Fail(ErrorCodes.InvalidArgument, message));
        };
    });
```

- [ ] **Step 4: 跑测试，确认通过**

```bash
DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
"$DOTNET" test Blog.Backend/Blog.Tests/Blog.Tests.csproj \
  --filter "FullyQualifiedName~UnifiedErrorResponseTests" 2>&1 | tail -15
```

Expected: 3 个用例全 PASS。

- [ ] **Step 5: 手工复核真实响应**

```bash
docker compose up -d --build webapi nginx
sleep 20
curl -sS -i "http://localhost:8080/api/posts/search" | tail -5
curl -sS "http://localhost:8080/api/posts/search?keyword=blog&page=1&pageSize=5" | head -c 150
```

Expected: 第一条返回 `{"code":4001,"message":"参数「keyword」不合法或缺失","data":null}`；
第二条仍是正常的 `{"code":0,...}`。

- [ ] **Step 6: 提交**

```bash
git add Blog.Backend/Blog.WebApi/Program.cs Blog.Backend/Blog.Tests/Integration/UnifiedErrorResponseTests.cs
git commit -m "fix(api): 接管模型校验响应，让「统一响应体」无条件成立

[ApiController] 的自动模型校验默认返回 RFC 7807 ProblemDetails，
与项目承诺的 {code,message,data} 并存两套结构。
实测缺口：GET /api/posts/search 缺 keyword 时返回 ProblemDetails。

新增 3 个端点级测试钉住该契约（含一条正常路径的反向护栏）。"
```

---

### Task 2: 窄屏侧栏残留

**Files:**
- Modify: `Blog.FrontEnd/src/layouts/AdminLayout.vue`（`@media (max-width: 880px)` 块内的 `.admin-side` 规则）

**Interfaces:**
- Consumes: 无
- Produces: 无（纯样式）

- [ ] **Step 1: 确认问题**

```bash
grep -n "position: sticky" -A 4 Blog.FrontEnd/src/layouts/AdminLayout.vue | head -8
grep -n "max-width: 880px" -A 14 Blog.FrontEnd/src/layouts/AdminLayout.vue
```

Expected: 桌面态 `.admin-side` 有 `position: sticky; height: 100vh`；
而 880px 媒体查询里只改了 `width/flex-direction/overflow-x`，**没有重置这两条** ——
于是窄屏下横向 tab 仍然按满视口高渲染。

- [ ] **Step 2: 修复**

在 `@media (max-width: 880px)` 的 `.admin-side { … }` 规则里补两行：

```css
  .admin-side {
    /* 桌面态的 sticky + 100vh 是为「侧栏独立滚动」服务的；
       窄屏折叠成横向 tab 后这个高度约束就成了 bug —— 横向 tab 会撑满整个视口。
       必须显式复位，媒体查询不会自动撤销其它规则。 */
    position: static;
    height: auto;
    width: 100%;
    flex-direction: row;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-3) var(--space-4);
    border-right: none;
    border-bottom: 1px solid var(--border-subtle);
    overflow-x: auto;
  }
```

- [ ] **Step 3: 验证**

```bash
cd Blog.FrontEnd && cmd.exe /c "npx vue-tsc -b && npx vite build" 2>&1 | tail -5
```

Expected: 构建通过（这条改动是 CSS，但要证明没破坏构建）。

人眼验收（必须做，自动化测不了"高度对不对"）：
把窗口缩到 <880px 打开 `/admin/posts`，横向 tab 高度应当**只占一行**，不再撑满视口。

- [ ] **Step 4: 提交**

```bash
git add Blog.FrontEnd/src/layouts/AdminLayout.vue
git commit -m "fix(frontend): 窄屏下侧栏高度约束未复位，横向 tab 会撑满视口

桌面态的 position:sticky + height:100vh 在 880px 断点里没有重置。
媒体查询只覆盖写在其中的属性，不会自动撤销其它规则。"
```

---

### Task 3: `.editorconfig` + 格式门禁（缺口 G6）

**Files:**
- Create: `.editorconfig`
- Modify: `.github/workflows/ci.yml`（hygiene 作业内，行尾检查之后）

**Interfaces:**
- Consumes: 现有 hygiene 作业（`ci.yml:47`）
- Produces: `dotnet format whitespace --verify-no-changes` 成为 CI 门禁

> **为什么只做 whitespace，不做完整 `--severity info`**
> 实测 `dotnet format --severity info` 在本仓库报 **21 条 analyzer 建议**
> （CA2016 / CA1873 / CA1816 / CA1861 / SYSLIB1045 / ASP0025）。
> 把它们一次性变成门禁 = 21 处改动 + 长期噪音，属于过度工程化。
> 本任务的目标只有一个：**让"格式"这件事由机器守，而不是靠人自觉**（`docs/09` §5.7 记录过两次提交事故）。

- [ ] **Step 1: 创建 `.editorconfig`**

```ini
# 本文件是编辑器的行为约定，也是 `dotnet format` 的输入。
# 目标：把 docs/07 §7.2 记录的「人肉纪律」变成机器强制。
#
# ⚠️ end_of_line 必须是 lf：
#    .gitattributes 的 `* text=auto eol=lf` 要求仓库内一律 LF，
#    CI 的 hygiene 作业会拒绝 w/crlf。
#    而 `dotnet format` 在本文件缺失时的**默认行尾是 CRLF** ——
#    不加这一行，格式化会把整个仓库改成 CRLF 并让 CI 变红。
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
indent_style = space
indent_size = 4

[*.{json,yml,yaml,md,ts,vue,js,mjs,css,html,scss}]
indent_size = 2

[*.md]
# Markdown 里行尾两个空格是有意义的换行；trim 会破坏排版
trim_trailing_whitespace = false

[*.cs]
# 与本项目既有代码风格一致（多数文件用文件级命名空间 + Allman 大括号）
csharp_new_line_before_open_brace = all
csharp_new_line_before_else = true
csharp_new_line_before_catch = true
csharp_new_line_before_finally = true
csharp_space_before_colon_in_inheritance_clause = true
indent_size = 4
```

- [ ] **Step 2: 看一次性格式化的影响面**

```bash
DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
"$DOTNET" format Blog.Backend/Blog.Backend.slnx whitespace --verify-no-changes 2>&1 | head -25
```

Expected: 约 20 条 `error WHITESPACE`（`Author.cs` 的 `class Author:` 缺空格、若干多行参数列表的换行）。

- [ ] **Step 3: 一次性格式化**

```bash
DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
"$DOTNET" format Blog.Backend/Blog.Backend.slnx whitespace
```

- [ ] **Step 4: 确认没有引入 CRLF（关键）**

```bash
git ls-files --eol | grep 'w/crlf' && echo "❌ 出现 CRLF，回滚并检查 .editorconfig 的 end_of_line" || echo "✅ 全部 LF"
git diff --stat
```

Expected: `✅ 全部 LF`；diff 只涉及少量文件、且**只有空白变化**（`git diff -w` 应当为空）。

```bash
git diff -w --stat   # 期望：空输出（说明没有任何非空白改动）
```

- [ ] **Step 5: 复跑测试**

```bash
DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
"$DOTNET" test Blog.Backend/Blog.Backend.slnx --configuration Release 2>&1 | tail -5
```

Expected: 与 Task 0 基线一致（通过数不变）。

- [ ] **Step 6: 加 CI 门禁**

在 `.github/workflows/ci.yml` 的 hygiene 作业里，"检查行尾是否被写成 CRLF" 之后追加：

```yaml
      # 缺口 G6：没有 lint / 格式门禁。
      # 只查 whitespace —— 完整 `--severity info` 会引入 21 条 analyzer 建议，
      # 属于过度工程化（见 plan/2026-09-14-大整理与上线方案.md §4）。
      # 覆盖范围由仓库根的 .editorconfig 决定（注意其中的 end_of_line = lf）。
      - name: 检查 C# 格式（dotnet format whitespace）
        run: |
          dotnet format Blog.Backend/Blog.Backend.slnx whitespace --verify-no-changes --no-restore \
            || { echo "::error::C# 格式不符合 .editorconfig，本地执行 'dotnet format Blog.Backend/Blog.Backend.slnx whitespace' 后提交"; exit 1; }
```

- [ ] **Step 7: 提交**

```bash
git add .editorconfig .github/workflows/ci.yml Blog.Backend
git status --short   # 确认没有误纳入优化的问题.txt / 简历 / TODO.md
git commit -m "chore: 加 .editorconfig 与格式门禁，关闭 G6

只门禁 whitespace：完整 --severity info 会引入 21 条 analyzer 建议，
属于过度工程化。.editorconfig 显式写 end_of_line=lf ——
dotnet format 默认是 CRLF，会与 .gitattributes 和 hygiene 作业冲突。"
```

---

### Task 4: 备份与恢复脚本进仓库

**Files:**
- Create: `tools/backup/backup.sh`
- Create: `tools/backup/restore.sh`
- Create: `tools/backup/README.md`

**Interfaces:**
- Consumes: `docker-compose.yml` 的服务名 `pgsql`、卷 `media`；环境变量 `POSTGRES_USER` / `POSTGRES_DB`（默认 `kky` / `blog_stage2`）
- Produces: `tools/backup/backup.sh <输出目录>` 生成 `blog_<时间戳>.sql.gz`；`tools/backup/restore.sh <备份文件>` 还原

> **为什么必须进仓库**：`docs/09` §6.4 的教训——"开发机上的文件不是项目资产"。
> 备份是**上线后唯一能救命**的东西，放在仓库外等于没有。

- [ ] **Step 1: 写备份脚本**

创建 `tools/backup/backup.sh`：

```bash
#!/usr/bin/env bash
# PostgreSQL 逻辑备份。在仓库根目录执行：bash tools/backup/backup.sh [输出目录]
#
# 为什么用 pg_dump 而不是直接拷数据卷：
#   卷是 PostgreSQL 的内部格式，跨版本/跨架构不保证可用；
#   pg_dump 产出的是 SQL，能读到、能改、能在任何 PG 上还原。
set -euo pipefail

OUT_DIR="${1:-./backups}"
STAMP="$(date +%Y%m%d-%H%M%S)"
DB="${POSTGRES_DB:-blog_stage2}"
USER="${POSTGRES_USER:-kky}"
FILE="${OUT_DIR}/blog_${DB}_${STAMP}.sql.gz"

mkdir -p "$OUT_DIR"

echo "==> 导出 ${DB} 到 ${FILE}"
# -T 关掉伪终端：不加的话 gzip 会写进 tty 而拿到空文件
docker compose exec -T pgsql pg_dump -U "$USER" -d "$DB" --no-owner --no-privileges \
  | gzip > "$FILE"

# 校验：小于 1KB 基本可以断定是空转（pg_dump 失败但管道仍返回 0 是经典陷阱）
SIZE=$(stat -c%s "$FILE" 2>/dev/null || stat -f%z "$FILE")
if [ "$SIZE" -lt 1024 ]; then
  echo "❌ 备份文件只有 ${SIZE} 字节，判定为失败：$FILE" >&2
  exit 1
fi

echo "✅ 完成：$FILE（${SIZE} 字节）"
echo
echo "⚠️ 备份没验证过 = 没有备份。请定期跑一次还原演练，见 tools/backup/README.md"
```

- [ ] **Step 2: 写还原脚本**

创建 `tools/backup/restore.sh`：

```bash
#!/usr/bin/env bash
# 从备份还原。在仓库根目录执行：bash tools/backup/restore.sh <备份文件> [目标库名]
#
# ⚠️ 这会**覆盖**目标库的现有数据。默认还原到 <库名>_restore 供核对，
#    确认无误后再手工改回正式库名。
set -euo pipefail

FILE="${1:?用法: bash tools/backup/restore.sh <备份文件> [目标库名]}"
SRC_DB="${POSTGRES_DB:-blog_stage2}"
TARGET_DB="${2:-${SRC_DB}_restore}"
USER="${POSTGRES_USER:-kky}"

[ -f "$FILE" ] || { echo "❌ 找不到备份文件：$FILE" >&2; exit 1; }

echo "==> 目标库：${TARGET_DB}（源：${SRC_DB}）"
echo "==> 重建目标库（先删后建，确保是干净还原）"
docker compose exec -T pgsql psql -U "$USER" -d postgres -v ON_ERROR_STOP=1 \
  -c "DROP DATABASE IF EXISTS \"${TARGET_DB}\";" \
  -c "CREATE DATABASE \"${TARGET_DB}\" OWNER \"${USER}\";"

echo "==> 灌入 ${FILE}"
gunzip -c "$FILE" | docker compose exec -T pgsql psql -U "$USER" -d "$TARGET_DB" -v ON_ERROR_STOP=1 > /dev/null

echo "==> 核对还原结果"
docker compose exec -T pgsql psql -U "$USER" -d "$TARGET_DB" -tAc \
  "select 'Posts=' || count(*) from \"Posts\";"
docker compose exec -T pgsql psql -U "$USER" -d "$TARGET_DB" -tAc \
  "select 'Users=' || count(*) from \"Users\";"
docker compose exec -T pgsql psql -U "$USER" -d "$TARGET_DB" -tAc \
  "select 'chinese 配置存在=' || count(*) from pg_ts_config where cfgname='chinese';"

echo "✅ 还原到 ${TARGET_DB} 完成。核对无误后："
echo "   DROP DATABASE \"${SRC_DB}\"; ALTER DATABASE \"${TARGET_DB}\" RENAME TO \"${SRC_DB}\";"
```

- [ ] **Step 3: 写 README（含恢复演练步骤）**

创建 `tools/backup/README.md`，内容必须包含：

1. **用法**：备份 / 还原各一条命令。
2. **备份什么、不备份什么**：数据库（有脚本）+ `media` 卷（上传的图片，用 `docker run --rm -v blog_media:/data -v "$PWD/backups:/out" alpine tar czf /out/media_<时间戳>.tar.gz -C /data .`）+ `.env`（**含密钥，单独保管，绝不入库**）。
3. **恢复演练（每季度一次）**：备份 → `restore.sh` 还原到 `_restore` 库 → 核对 Posts/Users/`chinese` 配置 → 用还原库启动一次应用 → 删除临时库。**"没演练过的备份等于没有备份"**。
4. **上线后的定时任务**：服务器上的 crontab 示例（每天 03:00 备份 + 保留 14 天）。

- [ ] **Step 4: 实测备份与还原（在本地 compose 栈上真跑一遍）**

```bash
chmod +x tools/backup/*.sh
bash tools/backup/backup.sh ./backups
ls -lh backups/
bash tools/backup/restore.sh "$(ls -t backups/*.sql.gz | head -1)"
```

Expected: 备份文件 > 1KB；还原后三行核对都打印出数字（`Posts=` / `Users=` / `chinese 配置存在=1`）。

- [ ] **Step 5: 清理演练产物并提交**

```bash
docker compose exec -T pgsql psql -U kky -d postgres -c 'DROP DATABASE IF EXISTS "blog_stage2_restore";'
echo "backups/" >> .gitignore
git add tools/backup .gitignore
git commit -m "chore(ops): 备份与还原脚本进仓库，并做了一次真实恢复演练

docs/09 §6.4 的教训：开发机上的文件不是项目资产。
脚本含空文件校验（pg_dump 失败但管道返回 0 是经典陷阱）与三行还原核对。"
```

---

### Task 5: 处理 11 个 Dependabot PR ★需要你（用户）在 GitHub 上操作

**Files:**

- 不改文件（除非某个 PR 需要修代码）

**Interfaces:**
- Consumes: GitHub 仓库 `whyitsy/blog-CI-CD` 的 PR #5~#15
- Produces: 开放的 Dependabot PR 数量归零

> **为什么这一步必须你做**：本机没有 `gh` CLI，也没有 GitHub token，我无法通过 API 合并 PR。

- [x] **Step 1: 先合并"安全"的那批（补丁/次要版本，CI 已绿）**

打开 https://github.com/whyitsy/blog-CI-CD/pulls ，按下面顺序合并（每个都等 CI 绿再合）：

| PR | 内容 | 判断 |
|---|---|---|
| #12 | marked 18.0.11 → 18.0.12 | ✅ 补丁，合并 |
| #6 | actions/setup-dotnet 4 → 6 | ✅ 合并 |
| #7 | actions/checkout 4 → 7 | ✅ 合并 |
| #8 | actions/upload-artifact 4 → 7 | ✅ 合并 |
| #5 | actions/setup-node 4 → 7 | ✅ 合并 |

> ⚠️ 合并 actions 类的 PR 后，**`.github/workflows/ci.yml` 里的版本号会被 Dependabot 改写**，
> 合并下一个 PR 时可能冲突 —— 一次合一个、每次都等 CI 绿。

- [ ] **Step 2: 逐个评估大版本跳跃（不要无脑合）**

| PR | 内容 | 风险 | 建议 |
|---|---|---|---|
| #15 | StackExchange.Redis 2.10.1 → **3.2.1** | 主版本跳跃，连接 API 可能有破坏性变更 | 先合到独立分支，本地跑全量测试 + 手工验证 Redis 缓存与限流降级 |
| #14 | 测试组（2 个包） | 低 | 合并 |
| #13 | microsoft 组（9 个包） | 中（ASP.NET Core / EF Core 相关） | 合并后必须跑全量测试 |
| #11 | @types/node 24 → **26** | 仅类型，但可能与 Node 24 不匹配 | 先确认 CI 用的 Node 版本，再合并 |
| #10 | vue-router 4.6.4 → **5.3.1** | 主版本跳跃，路由 API 可能破坏 | 单独分支 + 跑 `tools/e2e` 的 10 个检查 |
| #9 | 前端 build 组（**CI 失败**） | 未知 | 点进 PR 看失败日志；若是 vite/rolldown 不兼容，**关闭该 PR** 并在 `docs/06` 登记原因 |

- [ ] **Step 3: 把结论告诉我**

合并/关闭完成后告诉我，我会：
1. 重新核对 `docs/06`（原 `docs/09`）里 G5 依赖扫描的状态；
2. 若有 PR 被**关闭**，把"为什么关"记进 `docs/06`，避免下次又提。

---

## Task 5 执行结果（2026-09-14）

原计划列了 11 个 PR。实际结果如下——**两个 CI 失败的 PR 不是同一类原因**，
而且都属于"只修 lock 不够"的那种：

| PR | 内容 | 结果 |
|---|---|---|
| #5 #6 #7 #8 #12 | actions/* 与 marked | ✅ 已合并（Step 1） |
| #11 | @types/node 24 → 26 | ✅ 已合并 |
| #13 | microsoft 组（9 个包） | ✅ 已合并 |
| #14 | test 组 | ✅ 已合并 |
| #15 | StackExchange.Redis 2.10 → **3.2** | ✅ 已合并（主版本跳跃，CI 绿） |
| #18 | actions/cache 4 → 6 | ✅ CI 绿，可直接合并 |
| **#10** | vue-router 4.6 → **5.3** | 🔧 **已修 lock 并推回分支**，等 CI 复跑 |
| **#9** | typescript 6 → **7** + vite 8.2→8.3 | ❌ **不可合并**，已加 ignore 规则并登记进 `docs/06` §1.4 |

### #10：CI 失败的真因不是 vue-router，而是跨 PR 的 lock 漂移

Dependabot 把分支变基到 main 时，`package.json` 跟上了 main 的 `@types/node ^26.5.1`，
**`package-lock.json` 却停在 24.13.3**，于是 `npm ci` 直接拒绝执行：

```
Invalid: lock file's @types/node@24.13.3 does not satisfy @types/node@26.5.1
Invalid: lock file's undici-types@7.18.2 does not satisfy undici-types@8.9.0
```

修法：`npm install` 重生成 lock（53 增 / 10 删，`package.json` 未动），
本地 `npm ci` + `npm run build`（含 `vue-tsc` 类型检查）均通过，推回分支（`abc42d7`）。

> 💡 **这类失败会反复出现**：Dependabot 每个 PR 都改 `package-lock.json`，
> 而它们各自基于不同的 main 快照。**每合一个，其余的就可能变成不同步。**
> 判断口诀：CI 报 `npm ci ... in sync` → 一定是 lock 漂移，`npm install` 重生成即可，**不是代码问题**。

### #9：TypeScript 7 会让 `vue-tsc` 直接崩（与 lock 无关）

把 lock 修好、`npm ci` 通过之后，`npm run build` 仍然崩：

```
Error [ERR_PACKAGE_PATH_NOT_EXPORTED]: Package subpath './lib/tsc'
is not defined by "exports" in node_modules/typescript/package.json
    at resolveTscPath (node_modules/vue-tsc/index.js:73:43)
```

TS 7 是原生（Go）重写版，不再导出 `./lib/tsc`；而 `vue-tsc` 最新版就是仓库里已有的 **3.3.11**，
没有支持 7.x 的版本。**结论：这个 PR 现在合不了，不是配置问题。**

处理：`.github/dependabot.yml` 对 `typescript` 的 major 升级加了 `ignore`（带 `update-types`，
理由与写法同那里 `SixLabors.ImageSharp` 那条），完整证据登记进 [docs/06-技术债与待办.md](../docs/06-技术债与待办.md) §1.4。

⚠️ **注意**：`vue-tsc` 的 `peerDependencies` 写的是 `typescript: ">=5.0.0"`，
7.0.2 名义上满足——**所以它不会自己消失**，必须显式 ignore。

---

### Task 6: P0 收口

**Files:**
- Modify: `plan/2026-09-14-P0执行记录.md`

**Interfaces:**
- Consumes: Task 1~5 的全部产出
- Produces: 一个"P0 已完成"的、CI 全绿的 `main`

- [ ] **Step 1: 全量回归**

```bash
DOTNET="/mnt/c/Program Files/dotnet/dotnet.exe"
taskkill.exe /F /IM Blog.WebApi.exe 2>/dev/null || true
"$DOTNET" test Blog.Backend/Blog.Backend.slnx --configuration Release 2>&1 | tail -5
```

Expected: 失败 0，通过数 = Task 0 基线 **+ 3**（Task 1 新增三个用例）。

- [ ] **Step 2: 本地复现 CI 的全部检查**

```bash
# 1) 行尾
git ls-files --eol | grep 'w/crlf' && echo "❌ CRLF" || echo "✅ LF"
# 2) 格式
"/mnt/c/Program Files/dotnet/dotnet.exe" format Blog.Backend/Blog.Backend.slnx whitespace --verify-no-changes --no-restore && echo "✅ 格式 OK"
# 3) 前端构建
cd Blog.FrontEnd && cmd.exe /c "npm run build" 2>&1 | tail -3
```

- [ ] **Step 3: 推送并开 PR**

```bash
git checkout -b chore/p0-cleanup   # 若已在别的分支，先切回 main 再拉最新
git push -u origin chore/p0-cleanup
```

然后在 GitHub 上开 PR → 等 4 个作业全绿 → 合并。

- [ ] **Step 4: 写执行记录**

在 `plan/2026-09-14-P0执行记录.md` 记录：基线测试数、每项改动的文件、CI 运行链接、遇到的意外。
**这份记录是 P1 文档重构的事实来源之一**（例如 Task 1 会让 `docs/03` §2.1 的"所有接口"从"不准确"变成"准确"）。

---

## Self-Review

**1. Spec coverage**

| 方案 §4 的 P0 项 | 对应任务 |
|---|---|
| 1 统一响应体缺口 | Task 1 |
| 2 11 个 Dependabot PR | Task 5 |
| 3 公开的种子管理员密码 | **不在 P0** —— 它是部署流程的强制步骤，写进 P2 的 runbook（方案 §5.3 第 7 步） |
| 4 窄屏侧栏残留 | Task 2 |
| 5 备份脚本进仓库 | Task 4 |
| 6 `.editorconfig` | Task 3 |
| 7 Markdown 图片功能 | 已决定**上线后做**，登记在 `docs/06`（`docs/06` 属 P1 产出） |

**2. Placeholder scan**：无 TBD / "稍后补充"；每个代码步骤都给了可直接粘贴的内容。

**3. Type consistency**：`ApiResponse.Fail(int, string)`（`Blog.Application.Common`）、`ErrorCodes.InvalidArgument` = 4001、`Codes.InvalidArgument` = 4001（测试侧）、`ApiClient.SendAsync(HttpMethod, string, string? token = null, object? body = null)`、`BlogApiFixture.CreateClient()` —— 均与仓库现状一致（已逐一读取确认）。
