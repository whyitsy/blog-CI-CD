# 2026-09-09 工作日志

## 上下文压缩与前后端联调验证

### 已完成
1. **上下文压缩**：将项目当前状态写入 `.workbuddy/memory/MEMORY.md`，包含技术栈、架构决策、环境备忘、启动命令、当前进度。
2. **环境恢复**：
   - 启动 Docker Desktop
   - 启动 pgsql、redis 容器
   - 启动后端 `http://localhost:5080`
   - 启动前端 dev server `http://localhost:5173`
3. **接口联调验证**：
   - 后端 7 个关键接口全部 200：/api/posts、/api/site/stats、/api/site/config、/api/site/social-links、/api/tags、/api/categories、/api/posts/archives
   - 前端 Vite proxy `/api` 正常转发
4. **功能修复**：
   - 发现详情页阅读时间显示 `NaN`：后端 `PostDetailDto` 缺少 `WordCount` 字段，前端计算 `wordCount/400` 时得到 `NaN`
   - 修复：在 `PostDetailDto` 与 `PostQueryRepository.GetDetailAsync` 中补充 `p.WordCount`
   - 杀掉锁 DLL 的后端进程，重新构建并启动，清空 Redis 缓存后验证 `wordCount` 已返回
5. **页面渲染验证**（Chrome headless 截图 / DOM dump）：
   - 首页 Hero、文章卡片、Footer 统计正常
   - 标签页 /tags、分类页 /categories、归档页 /archive 正常
   - 文章详情页 /post/:id 正常，阅读时间显示正确
   - 搜索结果页 /posts?keyword= 正常
6. **构建与提交**：
   - 前端 `npm run build` 通过
   - 后端 `dotnet build` 0 警告 0 错误
   - 提交：`29f5696 fix(backend): PostDetailDto 返回 WordCount...`

### 待办（非基础功能，可后续处理）
- 搜索弹窗交互需要真实浏览器手动验证
- 详情页 giscus 评论依赖 GitHub Discussions，本地无法加载属正常
- 当前测试数据：2 篇文章均带 C# 标签，无分类，导致分类墙显示 0 篇
