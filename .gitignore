# .NET
bin/
obj/
*.user
*.suo
.vs/
artifacts/
TestResults/

# Logs
logs/
*.log

# IDE
.idea/
*.swp

# Node (frontend, future)
node_modules/
dist/

# OS
Thumbs.db
.DS_Store

# Local env / secrets
# 说明：Vite 的环境文件分级如下，只有带 .local 的才可能含个人密钥，必须忽略；
# .env / .env.[mode] 是可共享的默认配置（**不得放密钥**），应入库。
# 例外见下方「仓库根的 /docker compose 环境文件」——那一份**装的是真实密钥**。
.env.local
.env.*.local

# 仓库根的 docker compose 环境文件（**含真实密钥，必须忽略**）
# 说明：开头的 / 表示只匹配仓库根目录，因此不会影响已入库的 Blog.FrontEnd/.env。
# 模板见 .env.example（不含密钥，可入库）。
/.env

# Uploaded media (runtime files)
Blog.Backend/Blog.WebApi/media/

# Personal task scratchpad (本地待办与提问，刻意不跟踪)
# 说明：TODO.md 是开发者个人的临时任务清单，我的问题.md 是阅读文档时随手记的
# 问题清单。两者的内容都是阶段性的、随时会被改写，不适合作为项目文档入库
# （正式文档在 docs/，结论应当沉淀进对应文档）。
TODO.md
我的问题.md
