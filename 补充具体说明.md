# 补充技术说明与验收标准

## 1. 已有代码与工程信息
- 当前已实现模块：仅完成数据库实体与部分后端骨架，还需要按需增加实体。
- 目标目录结构遵循整洁架构：
  - Blog.Backend
    - Blog.Domain
    - Blog.Application
    - Blog.Infrastructure
    - Blog.WebApi
  - Blog.Frontend
- 数据库：PostgreSQL，连接字符串在 appsettings.Development.json 中。
- 如你发现现有代码与需求冲突，请先提出修改建议，不要直接推翻重写。

## 2. 前端技术要求
- 必须使用 Vue3 + TypeScript + Vite + Vue Router + Pinia。
- UI 组件库可选用 Naive UI
- 样式方案使用 SCSS 或原生 CSS，不使用 Tailwind。
- 动画优先使用 CSS Animation/Transition，打字机效果可自行封装指令。
- 所有动画需适配 `prefers-reduced-motion: reduce`。
- 响应式断点：
  - 桌面：≥1200px 三列卡片
  - 平板：768px–1199px 两列卡片
  - 手机：<768px 单列卡片
- 首屏背景图可从 `/src/assets/hero-bg.jpg` 读取，若不存在则使用纯色渐变背景。
- 文章封面图若为空，使用占位图。
- 搜索交互：点击导航栏搜索按钮后弹出搜索弹窗，输入关键词，防抖 300ms 后展示结果列表。
- 标签/分类点击后跳转到独立结果页，复用文章卡片列表组件。
- 卡片没有加载完成时采用骨架显示。

## 3. 数据模型约定

可能需要增加一些实体，如下等等。

+ SocialLink 社交链接表

+ SiteConfig 网站配置表

## 4. API 设计草案
采用统一的数据返回格式：

```json
{
  "code": 0,
  "data": {},
  "message": "ok"
}
```

规划和整理各类的api接口。

## 5. 日志方案

- 采用 Serilog。
- 输出：控制台 + 文件 `/logs/blog-.log`。
- 记录请求开始/结束、异常堆栈、慢查询（超过 500ms）、缓存命中率、限流触发。



## 6. 任务规划要求

请先输出以下文档，再开始编码：

1. 需求分析与实现方案及细节
2. API 完整清单
3. 后端任务拆分和开发顺序
4. 前端页面路由规划

文档保存在 `/docs` 目录下。