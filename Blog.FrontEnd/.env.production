# 生产环境（vite build，mode=production）
#
# 由 Nginx 把 /api 反代到后端，因此与开发一样留空即可（同源，无 CORS）。
# 若前后端**不同域**，在此填完整地址，例如：
#   VITE_API_BASE=https://api.example.com
VITE_API_BASE=

# 生产不使用 Vite 代理（由 Nginx 承担），留空即可。
# 配置了也会被下方 vite.config.ts 忽略（仅 dev server 生效）。
VITE_PROXY_TARGET=
