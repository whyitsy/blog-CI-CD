# 开发环境（vite dev，mode=development）
#
# VITE_API_BASE 留空 -> 请求走同源（localhost:5173）+ Vite 代理，
# 这样浏览器认为请求同源，完全避免 CORS 预检，相对路径的图片也能直接用。
VITE_API_BASE=

# Vite 代理的后端目标。改后端端口只需改这一行，不用动 vite.config.ts
VITE_PROXY_TARGET=http://localhost:5131
